<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/colorBackgroundLight"
                android:centerColor="@color/colorBackgroundLight"
                android:endColor="@color/colorBackgroundLight"
                />
            <corners android:radius="5dp" />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/playListInfoText"
                    android:centerColor="@color/playListInfoText"
                    android:endColor="@color/playListInfoText"
                    />
                <corners android:radius="5dp" />
            </shape>
        </clip>
    </item>
</layer-list>